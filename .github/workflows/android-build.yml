name: Android Debug Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: write

env:
  GODOT_VERSION: "4.5"
  EXPORT_NAME: opengsgproto

jobs:
  export-android-debug:
    name: Export Android (debug)
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:4.5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare export templates
        run: |
          mkdir -v -p ~/.local/share/godot/export_templates/
          mkdir -v -p ~/.config/
          mv /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/${GODOT_VERSION}.stable
          mv /root/.config/godot ~/.config/godot

      - name: Make build dir
        run: mkdir -v -p build/android ci-logs

      - name: Export debug APK
        id: export
        shell: bash
        run: |
          set -o pipefail
          godot --headless --verbose --export-debug "Android" ./build/android/${EXPORT_NAME}.apk 2>&1 | tee ci-logs/last-export-log.txt

      - name: Save log on failure
        if: failure()
        shell: bash
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config user.email "ci@opengsgproto.local"
          git config user.name "CI Log Bot"
          git add ci-logs/last-export-log.txt
          git commit -m "ci: capture failed export log [skip ci]" || echo "nothing to commit"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" HEAD:${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.EXPORT_NAME }}-debug-apk
          path: build/android/${{ env.EXPORT_NAME }}.apk
          if-no-files-found: error

